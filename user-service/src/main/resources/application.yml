server:
  port: 8899
spring:
  cache:
    cache-names: userCache
    type: redis
  # cloud:
#   sentinel:
#      transport:
#       dashboard: localhost:8080
#        port: 8719
#  profiles:
#    active: dev #开发环境
  datasource:
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
    url: jdbc:p6spy:mysql://localhost:3306/wgp?serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  redis:
    database: 0
    host: localhost
    port: 6379

logging:
  level:
    org.lhq.dao: debug