spring:
  application:
    name: file-service
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: master:8848
        file-extension: yaml
        group: DEV_GROUP
        namespace: Dev
        prefix: file
      server-addr: master:8848