server:
  port: 8889
spring:
  cloud:
    sentinel:
      transport:
        dashboard: localhost:8080
        port: 8719
    stream:
      kafka:
        binder:
          brokers: localhost:9092
          #zk-nodes: localhost:2181
          auto-create-topics: true
        bindings:
          #无论是生产者还是消费者，要接收到的对应的消息bindings下面的名称要保持一致
          myMq:
            destination: demo
